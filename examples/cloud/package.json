{
  "name": "scormhost-cloud-example",
  "private": true,
  "description": "FastAPI Cloud example with bundled LXPack demo course",
  "scripts": {
    "build:course": "cd course && ../node_modules/.bin/lxpack build --target scorm12 && cp .lxpack/security-awareness-scorm12.zip ../bundled/"
  },
  "devDependencies": {
    "@lxpack/cli": "^0.3.5"
  }
}
